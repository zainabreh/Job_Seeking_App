const initialState = {
    category:[]
}

const categorySlice = createSlice({
    name: "Category",
    initialState,
    reducers: {
        setCategory: (state,action)=>{
            
        }
    }
})